# TODOS — 23 Films

## Раунд 3a — следующая сессия
- **Поиск по уже добавленным тайтлам + дедуп.** Заменить строку добавления на поиск: сначала ищет среди уже добавленных тайтлов (нечёткое совпадение по name+year+kind — не нашёл, предлагает добавить новый через текущую форму). Заодно чинит незаявленную, но реальную проблему — сейчас два человека могут добавить один и тот же фильм дважды, раздваивая голоса. `UNIQUE(name, year, kind)` на `titles` как БД-бэкстоп от гонки при одновременном добавлении. Debounce на инпут поиска (~150-250мс). Без внешних API — TMDB отдельно, см. бэклог.

## Бэклог — не горит
- **TMDB-автозаполнение (год/постер).** Планировалось как Round 3b поверх поиска из 3a. **Отложено (2026-07-10):** есть вопрос с русскими названиями — TMDB ищет в основном по оригинальным/английским тайтлам, и неясно, насколько хорошо он матчит русские варианты («Матрица» → The Matrix) без доп. настройки (language-параметр, транслитерация). Разобраться с этим до реализации, не после — иначе фича будет работать хуже для 95% контента сайта (русского), чем задумано. Постеры по-прежнему планируются хотлинком на CDN TMDB — без своего хранилища.
- **Кириллический шрифт-компаньон.** Fraunces/Newsreader не поддерживают кириллицу, сейчас Georgia-фолбэк (осознанно, автор одобрил). Варианты: (а) оставить Georgia, (б) кириллический serif близкого характера через `unicode-range`, (в) кастомный патч шрифта (вряд ли оправдано для пет-проекта). Не менять на кириллический шрифт целиком — уже отклонялось трижды.
- **Персональные профили с историей просмотров** (v2).

## Completed
- ~~Убрать поле "год" из формы добавления~~, ~~переписать тег-лайн~~ — Round 1. **Shipped:** commit `0541d1a` (2026-07-10).
- ~~Анонимный юзер видит реальный список без кнопки голосования~~ — Round 2a. **Shipped:** commit `0541d1a` (2026-07-10).
- ~~Редактирование своих тайтлов~~ — Round 2b. **Shipped:** commit `4ab0b15` (2026-07-10).

## Cut (2026-07-10) — не делаем, если не появится реальный инцидент
- ~~Email-allowlist гейт~~ и ~~полная инвайт-система~~. Причина: Google-логин уже фильтрует случайных людей, ссылка приватная (не постилась публично), ноль инцидентов с накруткой/спамом с момента запуска. Правки RLS ради гипотетического риска — больше шанс случайно закрыть доступ всем, чем реальная защита. Пересмотреть, если: ссылка утечёт публично, или увидишь подозрительные голоса/тайтлы.
