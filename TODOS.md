# TODOS — 23 Films

## Раунд 1 — мелкие правки (готовы делать прямо сейчас)
- **Убрать поле "год" из формы добавления тайтла.** Люди почти никогда не помнят год выхода при добавлении — временно убираем поле, вернётся автозаполненным, когда придёт TMDB (см. Раунд 3).
- **Переписать тег-лайн на главной.** Текущий "Что смотрит наша тусовка" ощущается как сленг. Нужна подпись-CTA/объяснение того, как сайт работает — без панибратского тона.

## Раунд 2 — RLS + UI, эта неделя
- **Анонимный юзер видит реальный список без кнопки голосования** (вместо плейсхолдера "Войди, чтобы увидеть топ"). Требует открыть anon-SELECT на `titles_with_vote_counts` в RLS; insert/delete голосов остаются authenticated-only, накрутка не страдает.
- **Редактирование своих тайтлов.** Сейчас единственный способ исправить опечатку — вручную в БД руками автора. RLS UPDATE-политика: юзер может редактировать только тайтлы, где `added_by = auth.uid()`.

## Раунд 3 — отдельная сессия, самый большой прирост "вау"
- **Поиск по тайтлам + TMDB-автозаполнение.** Заменить строку добавления на поиск: сначала ищет среди уже добавленных тайтлов (не нашёл — предлагает добавить новый). Заодно чинит незаявленную, но реальную проблему — сейчас два человека могут добавить один и тот же фильм дважды, раздваивая голоса. TMDB-часть добавляет год/постер автоматически поверх того же UI.

## Бэклог — не горит
- **Кириллический шрифт-компаньон.** Fraunces/Newsreader не поддерживают кириллицу, сейчас Georgia-фолбэк (осознанно, автор одобрил). Варианты: (а) оставить Georgia, (б) кириллический serif близкого характера через `unicode-range`, (в) кастомный патч шрифта (вряд ли оправдано для пет-проекта). Не менять на кириллический шрифт целиком — уже отклонялось трижды.
- **Персональные профили с историей просмотров** (v2).

## Cut (2026-07-10) — не делаем, если не появится реальный инцидент
- ~~Email-allowlist гейт~~ и ~~полная инвайт-система~~. Причина: Google-логин уже фильтрует случайных людей, ссылка приватная (не постилась публично), ноль инцидентов с накруткой/спамом с момента запуска. Правки RLS ради гипотетического риска — больше шанс случайно закрыть доступ всем, чем реальная защита. Пересмотреть, если: ссылка утечёт публично, или увидишь подозрительные голоса/тайтлы.
